/.phpunit.cache
/node_modules
# public/build (compiled Vite assets) is intentionally tracked: this dev
# environment has no Node.js/npm, and vite.config.js pulls in many exact
# node_modules paths (some via git URLs for old packages), so the build
# is not reliably reproducible here yet. Revisit once that's set up.
/public/hot
/public/storage
/storage/*.key
/vendor
.env
.env.backup
.env.production
.phpunit.result.cache
Homestead.json
Homestead.yaml
auth.json
npm-debug.log
yarn-error.log
/.fleet
/.idea
/.vscode
